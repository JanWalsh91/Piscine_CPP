#include "Fixed.hpp"
#include <iostream>

int    main ( int ac, char** av ) {
	// TODO WHOEL THING
	// todo test getRaw functions
	// output not exactly the same
	// not using roundf
	
	if (ac == 1) {
		return (0);
	}


	return (0);
}